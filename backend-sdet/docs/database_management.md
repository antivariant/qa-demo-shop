# Database Management (SDET)

The SDET backend only stores user profiles in the `sdet_user` collection.

There are no seed or sync scripts in this backend. Use the Firebase Emulator UI or Firestore console for manual inspection when needed.
